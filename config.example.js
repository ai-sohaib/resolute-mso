window.RESOLUTE_CONFIG = {
  supabaseUrl: 'https://YOUR-PROJECT.supabase.co',
  supabaseAnonKey: 'YOUR_PUBLIC_ANON_KEY',
  formEndpoint: 'https://your-secure-form-endpoint.example.com/resolute-mso'
};

// Direct website email delivery needs a secure endpoint.
// GitHub Pages is static, so it cannot send email by itself.

